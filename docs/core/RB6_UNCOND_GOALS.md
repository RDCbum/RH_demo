# RB6 remaining goals (RH_Unconditional)

After wiring `ctx_real`/A2 from gate data and treating A1 as a mode-implies-A1 lemma, the open obligations are:

- `AxiomsShimAccepted`
- `NumericCoverageAlpha Alpha.GeneratedRMSContext.ctx_real` (i.e. `Alpha.RMS_envelope_closed ctx_real`)
